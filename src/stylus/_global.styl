html
    height 100%

body
    background url(../images/bg.gif) repeat-y
    background-color $darkGray
    color $white
    overflow-x hidden
    height 100%
    width 100%

    a
        $pointer()
        color $darkGray
        text-decoration none

        &:hover
            color $blueDevWay
